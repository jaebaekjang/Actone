# ACTONE DESIGN QA

2026-07-08 — V3 디자인 맨데이트 구현 후 비주얼 QA 기록.

## 환경

- 로컬 Supabase 호환 스택(e2e 하네스: PostgreSQL 16 + gateway.mjs) 위에서
  실제 Next.js 앱(웹 :3000, 관리자 :3001)을 dev 모드로 구동.
- Playwright(Chromium)로 캡처: `e2e/design-shots.mjs` (재실행 가능).
  ```bash
  # e2e/README.md의 스택 기동 후
  cd e2e && PGPASSWORD=<pw> E2E_PGPORT=5432 node design-shots.mjs
  ```
- 픽스처: 정회원+관리자 계정 1개(무대뒤), seed 8카테고리/샘플 글, 모임 상세
  (날짜/시간/장소/신청 링크), 댓글 2건, 대기 신고 2건.

## 검수 뷰포트

- 390 × 844 (모바일)
- 768 × 1024 (태블릿)
- 1440 × 1000 (데스크톱)

## 스크린샷 (docs/design-screenshots/)

각 페이지 × 3 뷰포트, `<page>-<width>.png` (전체 페이지), 랜딩은 첫 화면
전용 `landing-hero-<width>.png` 추가.

- `landing-*`, `landing-hero-*` — 빈 무대 스포트라이트 히어로 포함 랜딩
- `community-home-*` — 커뮤니티 홈 (데스크톱 피드+우측 레일)
- `board-meetups-*` — 오프라인 모임 게시판 (행 기반 목록)
- `post-detail-*` — 글 상세 + 댓글 (배우 생존방 샘플)
- `post-meetup-*` — 모임 글 상세 (상세 패널 + 신청 버튼, 정회원 시점)
- `write-*` — 글쓰기 (모임 카테고리 선택 상태, 모임 정보 섹션 포함)
- `mypage-*` — 마이페이지
- `admin-members-*`, `admin-reports-*` — 관리자 회원/신고 관리

## 히어로 체크리스트 (V2 추가 요청 §2)

- [x] 첫 화면에서 빈 무대가 즉시 인식됨 (1440/768/390 모두)
- [x] 위에서 내려오는 단일 스포트라이트 광선이 명확함 (cone + blur)
- [x] 바닥 타원형 빛 웅덩이 + 무대 경계선 + 빛 안에서만 보이는 마룻바닥 라인
- [x] 카피/CTA가 좌측 어두운 여백에 있어 빛 웅덩이를 가리지 않음
- [x] 모바일에서도 무대 바닥과 빛의 원 유지 (`pb-[28svh]`) — 검은 그라데이션으로
      잘려 보이지 않음
- [x] 사람/실루엣/관객/카메라/네온/3D/연기 없음, 호러·콘서트·클럽 분위기 아님
- [x] 전부 CSS 그라디언트 — 이미지·외부 요청 0, 성능 문제 없음

## 발견된 문제와 수정

| # | 문제 | 수정 |
|---|---|---|
| 1 | 히어로 서브카피 한국어 단어 중간 줄바꿈 ("생존 이/야기") | `body { word-break: keep-all }` 전역 적용 (web/admin globals.css) |
| 2 | 글쓰기 페이지 "오프라인 모임 정보" fieldset 레이아웃 붕괴 (라벨 세로 낱자, 입력칸 축소) | fieldset/legend → `section + h2(aria-labelledby)` 구조로 교체 (post-form.tsx) |
| 3 | 모임 날짜가 ISO 타임스탬프로 노출 ("2026-07-22T00:00:00.000Z") | `formatDateOnly()` 유틸 추가 후 post-row/featured-meetup/글 상세에 적용 |
| 4 | `formatDateTime` "AM 02:29" — 한국어 UI에 어색한 12시간제 | `hour12: false`로 24시간제 변경 |
| 5 | 커뮤니티 홈 공지 라벨 "공지"가 모바일에서 세로로 낱자 줄바꿈 | 라벨에 `shrink-0` 추가 |
| 6 | (구성 변경) 사용자 요청: donghaeng.club식 모임 중심 구성 | 랜딩 섹션 순서 재배치 — 오프라인 모임 밴드를 히어로 바로 아래로 |

수정 후 전 페이지 재캡처 완료 (docs/design-screenshots/ 최신본).

## 항목별 검수 결과

- 위계/정렬/간격: 모든 화면 4px/8px 그리드, 괘선 기반 구분 일관 ✓
- 라인 길이: 아티클 720–760px, 게시판 860px ✓
- 한국어 줄바꿈: keep-all 적용 후 낱자/단어 분리 없음 ✓
- 대비: 다크(#F2F0EC/#AAA7A1 on #111112), 라이트(#1E1D1A/#6B6862 on #F5F4F1) AA ✓
- 오버플로: 390px 가로 스크롤 없음 (footer 주소 포함) ✓
- 모바일 내비: 하단 4탭 + 카테고리 스크롤러 동작 ✓ (fullPage 캡처에서 고정
  요소가 중간에 겹쳐 보이는 것은 스크린샷 아티팩트, 실제 뷰포트에선 하단 고정)
- 카드 남용: 게시판/목록 전부 행 기반, 카드 그리드 없음 ✓
- 배지 일관성: 신규/정회원/튜터(골드+별)/운영진 동일 문법, 웹·관리자 모두 ✓
- 일반 AI 템플릿 느낌: 아이콘-제목-설명 카드/네온/블롭/글래스 없음 ✓
- 관리자: 라이트 운영 UI로 사용자 사이트와 명확히 분리 ✓

## 리디자인 후 회귀 확인 (2026-07-09)

- 브라우저 E2E 전체 재실행: **49/49 PASS** — 리디자인이 게스트 차단, 온보딩,
  CRUD, 좋아요/북마크/신고, 모임 링크 게이팅, 관리자 플로우를 깨지 않음.
- 모임 날짜에 요일 표기 추가됨 (`formatDateOnly` → "YYYY-MM-DD (수)").
- 주의: dev 서버 실행 중 `next build`를 돌리면 `.next`가 덮여 dev 청크 404로
  하이드레이션이 깨진다. E2E 전에 dev 서버를 재시작할 것.

## 남은 문제 (다음 모델)

- dev 모드 캡처라 Next.js dev 배지("N")가 좌하단에 찍혀 있음 — 프로덕션 빌드
  캡처로 교체하면 사라짐. 레이아웃 영향 없음.
- 모임 날짜에 요일 표기(예: 7/22 (수))를 붙이면 더 읽기 좋음 — 선택.
- 실 Supabase 인스턴스/카카오 OAuth 연결 후 아바타 이미지 포함 화면 재검수 필요.
- 튜터 배지가 실제로 보이는 화면(튜터 계정 픽스처) 캡처는 미포함 — E2E(49건)에서
  기능 검증은 완료.
